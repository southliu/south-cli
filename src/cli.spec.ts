import { test, expect } from 'vitest'

test('cli', () => {
  expect(true).toBe(true)
})

// 获取语言
test('获取语言', () => {
  // 执行获取语言
  // getLanguage()
})

// create-page
test('通过create-page生成页面', () => {
  // 执行创建方法
  // createPage(language)
})